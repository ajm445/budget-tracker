// Services will be added here
// Each service will be refactored to accept Supabase client as a parameter
// This allows the same service logic to work with both web and mobile Supabase clients

// Example usage after refactoring:
// import { createTransactionService } from '@budget-tracker/shared/services';
// const transactionService = createTransactionService(supabase);
